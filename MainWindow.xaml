<Window x:Class="Wolpope.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:h="clr-namespace:Wolpope.Helpers"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        xmlns:scm="clr-namespace:System.ComponentModel;assembly=WindowsBase"
        xmlns:views="clr-namespace:Wolpope.Views"
        Title="Wolpope — Dashboard" Height="768" Width="1366"
        WindowStartupLocation="CenterScreen"
        Background="#FF11111B">


    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220" /> <!-- ЛЕВОЕ МЕНЮ -->
            <ColumnDefinition Width="*" />   <!-- ОСНОВНАЯ РАБОЧАЯ ОБЛАСТЬ -->
        </Grid.ColumnDefinitions>

        <!-- ════════════ 1. ЛЕВОЕ МЕНЮ ════════════ -->
        <Border Grid.Column="0" Background="{StaticResource SidebarBrush}" Padding="20">
            <StackPanel>
                <!-- Логотип приложения -->
                <Border CornerRadius="16" ClipToBounds="True" Height="80" Margin="0,0,0,32">
                    <Border.Background>
                        <SolidColorBrush Color="{StaticResource CardColor}"/>
                    </Border.Background>
                    <Image Source="assets/logo.jpg" Stretch="Uniform" Margin="8"/>
                </Border>

                <!-- Навигация -->
                <RadioButton Content="📊 Dashboard" Style="{StaticResource MenuRadioButtonStyle}" 
                             IsChecked="{Binding CurrentView, Converter={StaticResource EqualityConverter}, ConverterParameter=Dashboard, Mode=TwoWay}"
                             Command="{Binding NavigateCommand}" CommandParameter="Dashboard" Margin="0,8"/>
                <RadioButton Content="❤️ Favorites" Style="{StaticResource MenuRadioButtonStyle}" 
                             IsChecked="{Binding CurrentView, Converter={StaticResource EqualityConverter}, ConverterParameter=Favorites, Mode=TwoWay}"
                             Command="{Binding NavigateCommand}" CommandParameter="Favorites" Margin="0,8"/>
                <RadioButton Content="⚙ Settings" Style="{StaticResource MenuRadioButtonStyle}" 
                             IsChecked="{Binding CurrentView, Converter={StaticResource EqualityConverter}, ConverterParameter=Settings, Mode=TwoWay}"
                             Command="{Binding NavigateCommand}" CommandParameter="Settings"/>

                <Border Height="1" Background="{StaticResource BorderBrush_}" Margin="16,32,16,16"/>
                <Button Style="{StaticResource MenuButtonStyle}" Content="📁 Wallpaper Cache" Command="{Binding OpenCacheFolderCommand}"/>
            </StackPanel>
        </Border>

        <!-- ════════════ 2. ОСНОВНАЯ РАБОЧАЯ ОБЛАСТЬ ════════════ -->
        <Grid Grid.Column="1">
            <views:DashboardView Visibility="{Binding CurrentView, Converter={StaticResource EqualityToVisibilityConverter}, ConverterParameter=Dashboard}" />
            <views:FavoritesView Visibility="{Binding CurrentView, Converter={StaticResource EqualityToVisibilityConverter}, ConverterParameter=Favorites}" />
            <views:SettingsView Visibility="{Binding CurrentView, Converter={StaticResource EqualityToVisibilityConverter}, ConverterParameter=Settings}" />
        </Grid>
    </Grid>
</Window>
